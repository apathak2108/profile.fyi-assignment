import React from "react";
import Layout from "@/components/layout";
import CheckoutContainer from "@/containers/checkout";

const CheckoutAdaptive = () => {
  return (
    <Layout>
      <CheckoutContainer />
    </Layout>
  );
};

export default CheckoutAdaptive;
